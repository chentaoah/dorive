### 实体名称匹配

```java
public static final Options ONLY_PRODUCT_CODE = Builder.build("Order(id)", "OrderDetail(orderId, productCode)");
```



### 实体类型匹配

```java
public static final Options ORDER_ORDER_DETAIL = Builder.build(Order.class, OrderDetail.class);
```



### 字段方法匹配

```java
public static final Options ORDER_ORDER_DETAIL1 = new Builder().match(Order.class).match(Order::getOrderDetail).build();
```



### 实体字段选取

```java
public static final String STRING_1 = "id";
public static final String STRING_2 = "orderId, productCode";
public static final Options ORDER_ORDER_DETAIL2 = new Builder().match(Order.class, OrderDetail.class).select(STRING_1, STRING_2).build();
public static final Options ORDER_ORDER_DETAIL3 = new Builder().match(Order.class).match(Order::getOrderDetail).select(STRING_1, STRING_2).build();
```

